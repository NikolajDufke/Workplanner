<Page
    x:Class="WorkPlanner.View.EmployeePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WorkPlanner.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:viewmodel="using:WorkPlanner.ViewModel"
    xmlns:Models="using:WorkPlanner.Model"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Page.DataContext>
            <viewmodel:EmployeePageViewModel/>
        </Page.DataContext>
   
    <Page.Resources>
        <Style x:Key="StackPanelStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="40"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="TextBlockStyle" TargetType="TextBlock">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Vertical" Background="LightBlue" BorderBrush="Black" BorderThickness="5">
            <TextBlock Style="{StaticResource TitleStyle}" Text="Personal Informations" VerticalAlignment="Top" Grid.ColumnSpan="2" Grid.Column="1"/>
                <TextBlock Text="FirstName:" Style="{Binding TextBlockStyle}"/>
                <TextBlock Text="{Binding EmployeeProp.FirstName}" Style="{StaticResource TextBlockStyle}"/>
                <TextBlock Text="LastName:" Style="{Binding TextBlockStyle}"/>
                <TextBlock Text="{Binding EmployeeProp.LastName}" Style="{StaticResource TextBlockStyle}"/>
                <TextBlock Text="Address:" Style="{Binding TextBlockStyle}"/>
                <TextBlock Text="{Binding EmployeeProp.Adress}" Style="{StaticResource TextBlockStyle}"/>
                <TextBlock Text="City:" Style="{Binding TextBlockStyle}"/>
                <TextBlock Text="{Binding EmployeeProp.City}" Style="{StaticResource TextBlockStyle}"/>
                <TextBlock Text="ZipPostal:" Style="{Binding TextBlockStyle}"/>
                <TextBlock Text="{Binding EmployeeProp.ZipPostal}" Style="{StaticResource TextBlockStyle}"/>
                <TextBlock Text="PhoneNumber:" Style="{Binding TextBlockStyle}"/>
                <TextBlock Text="{Binding EmployeeProp.PhoneNumber}" Style="{StaticResource TextBlockStyle}"/>
                <TextBlock Text="Email:" Style="{Binding TextBlockStyle}"/>
                <TextBlock Text="{Binding EmployeeProp.Email}" Style="{StaticResource TextBlockStyle}"/>
        </StackPanel>



    </Grid>
</Page>
